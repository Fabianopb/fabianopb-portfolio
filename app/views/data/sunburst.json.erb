{
	"name": "Skills",
	"children": [
		{
			"name": "Business",
			"children": [
				<% @business_skills.each_with_index do |business_skill, index| %>
					<% line = "{\"name\": \"#{business_skill.name}\", \"size\": #{business_skill.size}}" %>
					<%= index + 1 == @business_skills.length ? "#{line}".html_safe : "#{line},".html_safe %>
				<% end %>
			]
		},
		{
			"name": "Design",
			"children": [
				<% @design_skills.each_with_index do |design_skill, index| %>
					<% line = "{\"name\": \"#{design_skill.name}\", \"size\": #{design_skill.size}}" %>
					<%= index + 1 == @design_skills.length ? "#{line}".html_safe : "#{line},".html_safe %>
				<% end %>
			]
		},
		{
			"name": "Technology",
			"children": [
				<% @technology_skills.each_with_index do |technology_skill, index| %>
					<% line = "{\"name\": \"#{technology_skill.name}\", \"size\": #{technology_skill.size}}" %>
					<%= index + 1 == @technology_skills.length ? "#{line}".html_safe : "#{line},".html_safe %>
				<% end %>
			]
		}
	]
}